services:
  web:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/ngfw_matcher/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
